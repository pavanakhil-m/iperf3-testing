apiVersion: v1
kind: Service
metadata:
  name: iperf3-server-service-dell-16
  labels:
    app: iperf3-server-service-dell-16
spec:
  ports:
    - port: 5201
      protocol: TCP
      name: http
      targetPort: 5201
  selector:
      app: iperf3-server-dell-16
